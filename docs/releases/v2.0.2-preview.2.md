# Elevate Helper v2.0.2-preview.2

## Fixed

- published the WinUI app with self-contained Windows App SDK runtime files so fresh Windows machines no longer need to install Windows App Runtime 1.8 before launching the app
- pinned `Microsoft.WindowsAppSDK` to `1.8.260416003` for reproducible prerelease builds

## Validation

- `dotnet test .\tests\ElevateHelper.Tests\ElevateHelper.Tests.csproj -c Release`
- Windows release packaging is expected to run in GitHub Actions on branch push
