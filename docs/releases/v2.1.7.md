# Elevate Helper v2.1.7

- Report Excel and PDF files are now named using only the object title and revision number.
- Office reports retain the localized morning or lunch peak label from the ELVX job title.
- Removed internal batch folder names, path hashes, and English `morning`/`lunch` suffixes from report file names.
- Added regression coverage for regular reports and batch project layouts such as `Office/G1/morning`, `Office/G1/lunch`, and `Res/G2`.
