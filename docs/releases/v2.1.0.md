# Elevate Helper v2.1.0

- Fixed report generation for partially served stops when Elevate exports both Destination Call Stations and Floors Served rows.
- The report now prefers the actual `*_elvx.csv` export for the served-floor matrix while keeping `.elvx` as the main project source.
- Added regression coverage for groups where only some elevators serve lower parking floors such as `-2` and `-1`.
