# Elevate Helper v2.0.2-preview.3

## Fixed

- corrected elevator group report mapping after adding door width and opening type rows; start delay, door pre-opening, door open time, door close time, and light curtain delay now stay aligned with Elevate project values
- office runs now preserve completed peak folders instead of deleting existing results when adding another peak later
- morning-only office runs no longer delete an existing lunch folder
- completed office scenarios are skipped when all expected `.elvx`, `_elvx.csv`, and `batch_results.csv` files are already present

## Validation

- `dotnet test .\tests\ElevateHelper.Tests\ElevateHelper.Tests.csproj`
- Windows release packaging is expected to run in GitHub Actions on tag push
