# Elevate Helper v2.1.1

- Fixed progress detection for calculation files whose project name contains another number, for example `project g1 R07 01.elvx`.
- Progress now keeps `R07` as part of the project prefix and reads the trailing file index `01` as the current calculation number.
- Added regression coverage for the `project g1 R07 01.elvx` naming pattern.
