# Elevate Helper v2.0.2-preview.5

## Fixed

- the traffic-flow sheet now reapplies the bordered direction box after express-zone rows are collapsed
- the elevator-group sheet now prints dashes for express-zone and technical-floor rows instead of served-floor dots

## Validation

- `dotnet test tests/ElevateHelper.Tests/ElevateHelper.Tests.csproj`
- Windows release packaging is expected to run in GitHub Actions on tag push
