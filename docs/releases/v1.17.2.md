# Elevate Helper v1.17.2

## Fixed

- fixed report generation when `batch_results.csv` points to a non-canonical folder and step CSV files must be found under the selected project root
- fixed report generation failures caused by Excel `NumberFormat` assignment on template-dependent inserted rows
- aligned report sheet row insertion with the original VBA copy-origin behavior to preserve template formatting more reliably

## Improved

- report generation now saves both `xlsx` and `pdf` outputs in the selected report folder
- added unit tests for step CSV resolution, report output path generation, and VBA-compatible step file naming
