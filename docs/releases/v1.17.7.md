# Elevate Helper v1.17.7

## Fixed

- included the `.example` folder in published builds so installed releases can find report templates and sample resources at runtime
- installer packages now contain the same `.example` assets as the portable release zip

## Improved

- publish output now carries `.example` automatically for future releases and local installer builds
