# Elevate Helper v2.0.2-preview.6

## Fixed

- Office morning and lunch reports now save to the selected project root instead of the `morning` or `lunch` scenario folder
- Office report file names now include the scenario suffix (`morning` or `lunch`) so separate peak reports do not overwrite each other

## Validation

- `dotnet test tests/ElevateHelper.Tests/ElevateHelper.Tests.csproj`
- Windows release packaging is expected to run in GitHub Actions on tag push
